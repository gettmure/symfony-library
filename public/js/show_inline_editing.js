const showBookNameEdit = () => {
    const button = document.getElementById('change-name');
    button.style.display = button.style.display === 'block' ? 'none' : 'block';
}

const showDescriptionEdit = () => {
    const button = document.getElementById('change-description');
    button.style.display = button.style.display === 'block' ? 'none' : 'block';
}

const showImageUrlEdit = () => {
    const button = document.getElementById('change-image');
    button.style.display = button.style.display === 'block' ? 'none' : 'block';
}

const showYearEdit = () => {
    const button = document.getElementById('change-year');
    button.style.display = button.style.display === 'block' ? 'none' : 'block';
}

const showAuthorsEdit = () => {
    const button = document.getElementById('change-authors');
    button.style.display = button.style.display === 'block' ? 'none' : 'block';
}