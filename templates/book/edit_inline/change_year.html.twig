<div class="modal fade" id="change-year-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Редактировать</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="input-group mb-3">
                    <div class="input-group-prepend">
                        <span class="input-group-text" id="inputGroup-sizing-default">Год публикации</span>
                    </div>
                    {{ form_start(yearForm) }}
                    {{ form_widget(yearForm.year, {
                        'attr': {
                            'class': 'form-control',
                        }
                    }) }}
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
                {{ form_widget(yearForm.submit_year, {
                    'label': 'Применить',
                    'attr': {
                        'class': 'btn btn-primary',
                    }
                }) }}
                {{ form_row(yearForm._token) }}
                {{ form_end(yearForm, {'render_rest': false}) }}
            </div>
        </div>
    </div>
</div>